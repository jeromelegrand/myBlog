<div class="form-group">
    <label for="title">Titre de l'article</label>
    <input type="text" class="form-control" name="title" id="title" placeholder="Titre..." required value="{{ article.title }}">
</div>
{% if 'title' in error %}
    <div class="alert alert-danger" role="alert">
        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
        <span class="sr-only">Error:</span>
        Petit malin, tu as désactivé la vérification client. Le titre est obligatoire.
    </div>
{% endif %}
<div class="form-group">
    <label for="author">Nom de l'auteur</label>
    <input type="text" class="form-control" name="author" id="author" placeholder="Auteur..." required value="{{ article.author }}">
</div>
{% if 'author' in error %}
    <div class="alert alert-danger" role="alert">
        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
        <span class="sr-only">Error:</span>
        Petit malin, tu as désactivé la vérification client. L'auteur est obligatoire.
    </div>
{% endif %}
<div class="form-group">
    <label for="message">Contenu de l'article</label>
    <textarea class="form-control" name="message" id="message" placeholder="Votre article..." required>{{ article.message }}</textarea>
</div>
{% if 'message' in error %}
    <div class="alert alert-danger" role="alert">
        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
        <span class="sr-only">Error:</span>
        Petit malin, tu as désactivé la vérification client. Le message est obligatoire.
    </div>
{% endif %}